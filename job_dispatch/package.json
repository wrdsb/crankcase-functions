{
    "name": "job_dispatch",
    "version": "0.0.1",
    "description": "Makes outbound HTTP requests based on ada-job-queue queue items, and updates queue items based on results.",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "",
    "license": "MIT",
    "dependencies": {
    }
}
